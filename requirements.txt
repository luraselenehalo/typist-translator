# UI shell (WebView2 on Windows 11 — the runtime ships with the OS)
pywebview>=5.0

# Backend: global hotkey, clipboard, tray icon, image handling
keyboard>=0.13.5
pyperclip>=1.11.0
pystray>=0.19.5
Pillow>=10.0.0
darkdetect>=0.8.0
